<source>
  type multiprocess

  # your original conf settings here.
  #<process>
  #  cmdline -c /etc/td-agent/your_orig_td_agent.conf
  #  sleep_before_start 1s
  #  sleep_before_shutdown 5s
  #</process>

  <process>
    cmdline -c /etc/td-agent/nefmon.conf
    sleep_before_start 1s
    sleep_before_shutdown 5s
  </process>

</source>
